import { AiOutlinePlusSquare } from "react-icons/ai";

export default function CreatePlaylist({ name, description, isPublic }) {
  return (
    <div>
      <h1>Create Playlist</h1>
      <AiOutlinePlusSquare size={40} />
      );
    </div>
  );
}
